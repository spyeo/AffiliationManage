<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coreplus.mapper.ProspectMapper">

<select id="selectList" resultType="com.coreplus.domain.ProspectVO">

<![CDATA[

select pros_id
     , pros_nm
     , cell_ph_no
     , cell_ph_tno
     , cell_ph_pno
     , eml_id
     , eml_domain
     
from     prospect
where    pros_nm like '%'||#{name}||'%'
order by pros_id

]]>

</select>

<select id="selectOne" resultType="com.coreplus.domain.ProspectVO">

<![CDATA[
select pros_id
     , pros_nm
     , cell_ph_no
     , cell_ph_tno
     , cell_ph_pno
     , eml_id
     , eml_domain

from     prospect
where    pros_id = #{pros_id}
]]>

</select>

<insert id="insertProspect">
<![CDATA[
insert into prospect(pros_id, del_yn, created_by, created, last_upd_by, last_upd, pros_nm, cell_ph_no, cell_ph_tno, cell_ph_pno, eml_id, eml_domain)
values(seq_prospect.nextval, 'n', #{created_by}, sysdate, #{last_upd_by}, sysdate, #{pros_nm}, #{cell_ph_no}, #{cell_ph_tno}, #{cell_ph_pno}, #{eml_id}, #{eml_domain})
]]>
</insert>



</mapper>